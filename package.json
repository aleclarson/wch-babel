{
  "name": "wch-babel",
  "version": "0.0.1",
  "main": "js/index",
  "scripts": {
    "build": "coffee-build src -o js",
    "prepublishOnly": "prepv -i || true"
  },
  "devDependencies": {
    "wch-coffee": "*"
  }
}
